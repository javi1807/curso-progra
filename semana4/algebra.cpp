#include <iostream>
using namespace std;

int main() {
    bool a = true;
    bool b = false;

    // Expresión booleana con operador AND
    bool resultado = a && b;
    
    cout << "a && b = " << resultado << endl; // Resultado: 0 (false)
    
    return 0;
}
